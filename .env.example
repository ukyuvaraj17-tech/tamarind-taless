# Supabase Config — get these from Supabase Dashboard > Project Settings > API
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Cloudinary — for image uploads (get from cloudinary.com)
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# Admin email — only this email can access /admin
REACT_APP_ADMIN_EMAIL=pc1@uktex.net

# Razorpay — get these from the Razorpay Dashboard > Settings > API Keys.
# NOTE: no REACT_APP_ prefix on either one — both are used only by the
# serverless functions in /api, never bundled into the public site. Also set
# these two under Vercel > Project > Settings > Environment Variables for
# production (a local .env file is never deployed).
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_key_secret
